{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Jourferie</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Datejf</th>
                <td>{{ entity.datejf|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('jourferie') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('jourferie_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('jourferie_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}



{% block body -%}
    <h1>Pays list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Codepays</th>
                <th>Nom</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pays_show', { 'id': entity.id }) }}">{{ entity.codepays }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pays_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pays_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pays_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
